# 更新记录

## v1.9

设置页滚动位置与尺寸反馈修复。

**修复**

- **切页签/切回应用都弹回顶部**：重画整页时内容全删了重加，滚动位置自然归零。原来只在切页签时补救，而且补救得太早（那一刻新内容还没量过，`ScrollView` 拿旧高度去钳，结果照样滚回顶部）。现在统一在重画结束、这一轮布局量完之后再还原，并且**所有**重画路径（切页签、换主题、切深浅模式、开关角色、从桌面切回来）都会保留当前位置
- **从桌面切回来整页重搭**：`onResume` 原来无条件重画一遍。现在只有「跟随系统的深浅模式在后台翻页了」才重画，其余情况轻量刷新（状态行 + 预览），滚动位置不会被顶掉
- **预览里角色大小拖动没反应**：预览中的角色尺寸是搭页面时一次性算好的 LayoutParams，改设置只更新了内存里的配置，画面不动 —— 看着就像「大小调不了」。现在尺寸、显隐、动作、外框高度都会跟着滑杆实时变
- **滑杆偶尔拖不动**：外面套着 `ScrollView`，手指横拖滑杆时稍带一点上下抖动，滚动就会把手势抢走，拖到一半断掉。现在按住滑杆期间明确禁止父容器拦截
- **圆角下限对不上**：滑杆允许 0，但存盘时被钳到 2，重进设置又会跳回 2。滑杆下限改成 2，文案同步

**变更**

- **页签栏固定到屏幕底部**：以前它长在滚动内容里，往下翻就看不见，想切页签得先滚回顶部 —— 那样「记住滚动位置」也没意义
- `versionCode` 11，`versionName` 1.9，包名与签名不变，可以直接覆盖安装

## v1.8

应用内检查更新：打开设置就能看到有没有新版，可以直接去下载。

**新增**

- **检查更新**（`UpdateChecker`）：设置页「关于」分页多一张卡片，显示当前版本、最近一次检查结果，配「立即检查」和「打开发布页」两个按钮
  - 自动检查每天最多一次（GitHub 匿名额度是每小时 60 次），用户点「立即检查」不受这个限制
  - 查到新版先弹提示：有 APK 资产就直接下 APK，没有就退到发布页；不想要的版本可以「跳过这个版本」，之后不再提醒同一个版本
  - 任何失败（断网、限流、返回的 JSON 不认识）都当成「没查到」：不弹错、不打扰，只在手动检查时回一句「网络不通或接口限流」
  - 发布说明来自 Release 正文，显示前压成一行并去掉 Markdown 的 `#`/`*`/反引号，不然弹窗里全是 `### 新增`
- **版本比较抽成纯逻辑**（`Version`）：tag 解析（`v1.7` / `1.7-rc1` → `1.7`）与按小数点分段比大小，不依赖网络也不依赖 Android，配套 `test/VersionTest.java`（65 项断言，已并入 `tools/run_tests.sh`）
- **设置页切分页不再跳回顶部**：每个分页记住自己的滚动位置
- **悬浮窗自己监听设置变化**（`BubbleService`）：改设置后由服务端监听 SharedPreferences 直接重排，不再依赖设置页推 Intent 过来（那个路径依赖「服务在跑」的标记，标记一旦失准就怎么改都不生效）；位置、余额这类程序自己写的键做了排除，免得回灌打架
- **启动悬浮窗改用 `startForegroundService`**：Android 8 起前台服务必须这么启，原来是 `startService`，退到后台再开会被系统拒掉

**修复**

- **`UpdateChecker.java` 缺 `package com.coco.balancebubble;`**：这个类是后来新加的，忘了写包声明就落在了默认包（未命名包）。Java 规定具名包里的类看不见默认包里的类，于是 `MainActivity` 里所有 `UpdateChecker.Info` / `UpdateChecker.PAGE_URL` 都被当成「包不存在」，编译报 5 处 `package UpdateChecker does not exist` / `cannot find symbol`。补上包声明即修好
- 顺手把「压成一行」做实：原来的 `shortNotes` 只去 `\r` 和首尾空格，换行照旧，名字与行为不符，而且没有调用点；现在真正折叠空白并接到弹窗上

**变更**

- `versionCode` 10，`versionName` 1.8，包名与签名不变，可以直接覆盖安装
- 仓库里的 `tools/manifest_gen.py` 版本号之前停在 1.6（v1.7 的版本号只改在构建目录、没进仓库），这次一并对齐到 1.8

## v1.7

设置页重做 + 局部形变手感修复 + 骨架模型收口。v1.6 只出过内部构建、没有发布，改动并入本条。

**设置页重做**

- **分页**：「连接 / 气泡 / 角色 / 关于」四个分页，36 张卡片，每张卡片一句说明写着「调这个会怎样」，不再是一条长列表从头拉到尾
- **实时预览置顶**：预览气泡和悬浮窗共用同一套渲染代码（`BubbleView` + `BubbleLayout`），改宽度、字号、底色会立刻看到结果，不用回桌面看
- **六套主题色 × 浅色/深色/跟随系统**（`Theme`）：颜色不再散落在各处的 `0xFF...`，全部走 `Ui` 这个控件工厂（卡片、开关、滑杆、色板、分段控件）
- **气泡可调项**：底色（自动 + 9 档色板）、不透明度、圆角、尖角长度与方向、内边距、基准字号与最小字号、最大/最小宽度、行数上限、柔和阴影、弹出动画
- **角色可调项**：大小、骨架动作开关、活跃度（安静 / 适中 / 活泼）、点击与长按分别做什么、自动说话节奏
- **配置导出/导入**：整份设置（含密钥）复制到剪贴板或从剪贴板导入，换机不用重填
- **排版引擎抽成纯逻辑**（`BubbleLayout`）：先按基准字号折行，折不下按比例缩字号，缩到最小就把行数放宽让气泡长高，最后才截断；「设置 → 渲染」只有 `Prefs → BubbleStyle` 一条通道，绘制层不再回读 SharedPreferences

**修复**

- **主题色上的文字对比度不够**：`Theme.inkOn` 原来按亮度阈值 0.55 二选一，对中间亮度的饱和色会挑错 —— 琥珀橙底色配白字只有 2.80:1，低于 WCAG 的 3:1 线（按钮、开关、分段控件的字都走这个函数）。改成在两个候选字色里挑对比度更高的那个：琥珀橙 2.80→5.97、薄荷绿 3.30→5.06、樱花粉 3.51→4.75，其余三档本来就选对、不受影响

- **挥手时手掌前缘被压扁**：手臂权重场的带宽是 30px、手掌场 53px，两场的权重梯度在手掌前缘叠乘，最紧的一帧最小伸缩掉到 0.45（判据 0.55），前缘看得出来发皱。手臂场带宽 30→56px（k 1.50→2.80），手掌场内半径 34→20px 而外径保持 86px（过渡带 53→66px，梯度降 21%）。压缩回到 0.63（面积 0.69、拉伸 1.32、剪切 0.33），手臂与手掌的「折痕余量」从 1.30× 提到 1.69×
  - 关键点：外径固定时「过渡带宽度 = 外径 − 内半径」，所以**内半径要小、k 要大**。旧的内半径 34px 比整只手掌（37×40）还大，白白把带宽压掉 13px；收到 20px 后掌上距心 27px 处仍有 0.97 权重，刚性和可见幅度都没变
- **呼吸只带走肚子，胸口和脖子一动不动**：1927 帧里胸口位移峰值只有 1.8px（判据 1.8，差一点点没到）、颈根 0.00px（判据 0.1），「上半身像纸片」的僵硬感就来自这里。呼吸场的上界从 y=440 提到 415，现在胸口 3.65px、颈根 0.67px
- **眨眼时左眼横向漂移**：左眼的横向收敛系数误用了右眼的 `eye_r_cx`，半闭时左眼会往右挪约 10px；修正后横移 0px
- **保护区改成径向软罩**：原来是 x/y 两条软边相乘，四角梯度叠成 2 倍，挥手时手部局部伸缩一度到 −1.13（网格整片翻转）
- 权重场带宽按伸缩预算反推（带宽 = k·R，压缩下限 ≈ 1 − 1.5·幅度/带宽），不再给固定带宽

**内部**

- 骨表、权重场形状、幅度上限、姿态表统一到 `tools/rig_math.py` 作为唯一事实来源；`app/src/.../RigModel.java` 由 `tools/gen_rig_java.py` 生成，验收会断言磁盘上的常量与生成器输出逐字节一致
- 验收套件重写成七节量化判据：位移场一致性、权重场平滑度（最大斜率 ≤0.06/px）、动作隔离、形变安全（轮廓内逐点伸缩 ∈[0.55, 1.80]、剪切 ≤0.80、渲染网格单元面积 ≥0.25×、单点位移 ≤125px）、可见性、39 状态 ×169 点黄金表、1927 帧动画时间线
- 新增 `test/RigGoldenTest.java`（Java 读黄金表逐点比对）与 `test/RigAnimTest.java`（在 Java 里按真实时间线播放动画并落盘采样），Java 与 Python 的逐帧位移偏差 0.0000px
- 新增 `test/SettingsKitTest.java`（366 项断言，已并入 `tools/run_tests.sh`）：主题配色的对比度（36 组颜色对，正文 ≥4.5:1、辅助文字 ≥3:1）、色板与下标越界、气泡排版的硬约束（宽度/行数/字号/每行行宽都不越界，未截断时文字不丢）、币种在「下标 ↔ 代号」之间的往返

**变更**

- `versionCode` 9，`versionName` 1.7，包名与签名不变，可以直接覆盖安装

## v1.5

角色局部形变：眨眼、发丝随风、甩尾、挥手。

**新增**

- **网格局部形变**（`PartRig`）：把贴图切成 26×26 网格，用一组权重场单独驱动脸、发丝、双手，比整体变换更像“活着”
  - **眨眼**：每 2.2~6 秒眨一次，眼睛纵向压下去，脸的其他部分不动
  - **发丝随风**：头顶与两侧长发按正弦波漂移，两个方向不同相位，不会像整体平移
  - **甩尾**（新动作）：两侧长发朝同一侧摆开，幅度越靠外侧越大，靠根部几乎不动
  - **挥手**（新动作）：右臂绕腕点转动，左手只跟着晃一点点；腕点几乎不动，手才不会脱体
- 待机随机动作池加入甩尾与挥手（动作共 13 种，待机池 10 种），并补了两句对应台词
- 脸部、双手是**保护区**：发丝和尾巴摆动时不会把脸和手一起拖走

**调整**

- 甩尾从“绕发根旋转”改成**有界横摆**：旋转的位移随离根距离线性增长，而长发一直垂到贴图底部，底端会被拽出上百像素、把网格拉翻（实测最小格宽 -26px）；横摆幅度封顶后形变梯度始终小于 1
- 发丝横向影响带加宽到 110px、纵向过渡 130px，过渡更柔
- 甩尾时风幅只放大到 1.6 倍（原来是 2.1 倍），避免和横摆叠加过猛
- 静止时顶点几乎不动，走普通绘制路径，不额外开销

**变更**

- `versionCode` 7，`versionName` 1.5，签名与包名不变，可直接覆盖安装
- 单测新增 `PartRigTest`：网格形状、权重场、保护区、眨眼/挥手/甩尾方向与幅度、形变连续性（网格不折叠）、240 帧稳定性

## v1.4

真实立绘、气泡自适应与三模式。

**新增**

- **换用原始立绘**：角色改用你上传的那张鲸鱼娘原图，抠图（去白底、边缘羽化）后作为透明贴图，不再使用此前的代码手绘造型
- **角色动作**：以贴图为整体骨架，绕脚底支点做倾斜、摇摆、挤压拉伸，跳跃时配地面阴影。内置漂浮、蹦跳、摇摆、歪头、摇头、点头、转圈、伸懒腰、缩一下、弹一下、打瞌睡 11 种动作
- **三种模式**：仅 token 查询 / 仅桌宠 / 混合模式
- **余额变化提醒**：混合模式下，后台静默刷新发现金额变了会主动冒个泡告诉你
- 「角色动作」开关：关掉角色就不动，可以省电
- **应用图标换成鲸鱼娘**：从原图抠出后居中排版成 432×432 图标

**调整**

- **气泡宽度跟着字数走**：短句是小气泡，长句自动折行、气泡变宽变高；折到第三行还放不下才缩字号，不再粗暴截断
- 气泡宽高变化加了 170ms 的补间动画，切换文字不再是硬跳变
- 角色渲染限到 30fps，并在窗口不可见时停止重绘
- 贴图与图标经 pngquant 量化，APK 从 479 KB 降到 283 KB

**变更**

- `versionCode` 6，`versionName` 1.4

## v1.3

桌宠模式与气泡交互调整。

**新增**

- 「token 查询模式」开关。关掉之后就是纯桌宠：点一下角色随机说一句卖萌话，并且每隔一分钟自动冒一句
- 应用更名为「鲸鱼娘桌宠」
- 查询余额时气泡里先显示「正在刷新中…」，拿到结果后再换成金额

**调整**

- 气泡平时收起，点一下角色才弹出来，7 秒后自动收回（点气泡可以立刻收起）
- 定时刷新改为静默执行，不再把气泡弹出来打扰你
- 设置页「③ 显示与刷新」改为「③ 模式与显示」，模式开关改完立刻生效，不用再点保存

## v1.2

界面重做与拖拽修复。

**修复**

- 拖动气泡松手后会弹回屏幕下方。原因是松手时先调用了按旧坐标定位的 `placeInitial()`，再保存新坐标，导致每次拖拽都被回滚
- 气泡长按和单击在拖动结束时可能误触发
- 金额文字变长或切换角色开关后，气泡可能被挤出屏幕边界

**新增**

- 卡片式设置界面：四张编号卡片 + 三步引导，每个输入框下方都有参数说明
- 实时效果预览：改币种、角色大小、显示名称时立刻看到气泡渲染结果
- 币种下拉框：15 种货币（默认人民币）+ 自动识别 + 自定义符号
- 「金额后补币种代码」开关，用于区分同为 ¥ 的人民币和日元
- 高级设置区，默认折叠，收纳 Base URL、接口路径、认证头、取值路径等参数
- 「把气泡放回默认位置」与「恢复默认参数」
- 网络与 HTTP 错误提示改为可操作的排查建议（401/404/429/超时等分别说明）
- 币种与预设的 JVM 纯逻辑单测 `test/CurrencyTest.java`

**变更**

- 未识别到的服务商名称归入「自定义」，不再回落到 DeepSeek
- `versionCode` 3，`versionName` 1.2

## v1.1

功能扩展与品牌调整。

**新增**

- 内置角色贴图 `assets/char.png`，气泡下方可显示角色
- 内置桌面图标 `mipmap/ic_launcher`（通过手写的 `resources.arsc` 注册）
- 服务商预设扩展为六家：DeepSeek、硅基流动、Moonshot、OpenRouter、OpenAI、自定义
- 金额字段路径留空时自动识别余额字段
- 币种留空时从接口返回的 `currency` 字段推断符号（CNY→¥ / USD→$ / EUR→€）
- 认证头名称与前缀可配置

**变更**

- 应用名「余额气泡」改为「token 余额查询器」
- `versionCode` 2，`versionName` 1.1

## v1.0

首个可用版本。

- 前台服务 + `TYPE_APPLICATION_OVERLAY` 悬浮气泡，对话气泡造型，下方尖角
- 拖动移动位置并持久化；单击刷新；长按打开设置
- 定时自动刷新，间隔可配置
- 开机自启（按上次状态恢复）
- DeepSeek 余额查询

---

## 关于早期版本的源码

v1.0 与 v1.1 只保留了签名后的 APK 产物，没有留存对应的源码快照。仓库中的 `app/src` 是 v1.2 的代码。如需查看早期实现，可以从 `releases/` 下的 APK 反编译。
